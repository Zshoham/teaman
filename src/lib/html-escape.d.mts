/** Escape a value being placed in text content. */
export function escapeHtml(value: string): string;

/** Escape a value being placed in a double-quoted attribute. */
export function escapeAttr(value: string): string;
