# Build output
public/
test-results/
playwright-report/
example/dist

# Dependencies
node_modules/

# Astro build cache
.astro/

# where `teaman build` stages a site before moving it to --out (disposable)
.teaman-out-*/

# Slidev workaround dir (see slidevjs/slidev#1975)
.slides-build/

# cached tikz/typst fence renders (remark-fence-svg — disposable)
.diagram-cache/

# cached reference PDFs, keyed by Typst source digest (disposable)
.reference-cache/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
